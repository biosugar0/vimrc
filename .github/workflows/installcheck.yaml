---
name: Install check
on:
  push:
    branches:
      - master
jobs:
  install:
    name: install check
    runs-on: macos-10.15
    steps:
      - uses: actions/checkout@master
      - name: vim setting
        run: make install
